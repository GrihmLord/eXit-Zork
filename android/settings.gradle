pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()
        // 👇 Tell Gradle to also look inside your node_modules!
        maven {
            url("$rootDir/../node_modules/react-native/android")
        }
    }
    plugins {
        id("com.facebook.react") version("0.72.17") apply(false)
    }
}

rootProject.name = "eXitZork"
include ':app'
